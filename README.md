# openshift-pipeline-badges

Stores pipeline status badge JSON files for Wakefern OpenShift CI/CD pipelines that are Updated automatically on each pipeline run and consumed by shields.io endpoint badges in application README files.
